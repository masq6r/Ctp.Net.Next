# Type Safety

> Not applicable for this repository.

Frontend-specific type-safety conventions do not apply here because the repository does not contain a TypeScript or browser-side codebase.

Type guidance for actual work belongs to the managed F# and native interop layers instead.
