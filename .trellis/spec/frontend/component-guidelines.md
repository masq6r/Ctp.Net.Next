# Component Guidelines

> Not applicable for this repository.

This repository has no frontend or UI component layer.

Do not assume React, Vue, Svelte, XAML, Razor components, or browser rendering concerns when working in this codebase.

If a real frontend package is added in the future, replace this file with package-specific conventions instead of reusing this placeholder.
