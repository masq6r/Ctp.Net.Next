# Hook Guidelines

> Not applicable for this repository.

This repository has no frontend runtime and no hook abstraction layer.

Do not invent `use*` conventions, client-side data-fetching hooks, or browser lifecycle rules for work in this codebase.
